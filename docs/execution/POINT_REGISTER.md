# Point-by-Point Completion Register — all 346 sub-points (MRS §0–§74)

**Date:** 2026-07-13. Accounts for every numbered sub-requirement so nothing is dropped. Per owner instruction, points that need **your words** (decisions, clinical content, off-site) are collected in **Part D (do last)**.

**Status legend:** ✅ DONE (executed + build-verified) · 📄 DELIVERED (spec/doc artifact produced) · 🛠️ SCHEDULED (autonomous code, no input needed) · 🧑 NEEDS-YOU (decision / clinical / off-site).

**Tally:** 346 sub-points → ✅ ~56 · 📄 ~150 · 🛠️ ~47 · 🧑 ~93. (Every point maps to one bucket below.)

**Update 2026-07-13 (round 2):** executed §44.5 orphan checker, §10.5 glossary (288 terms), §42.6+§7 `.htaccess` capture, §43.5 audit, §40.1/§7.6 audit, §72.1 JSON-LD gate + CI. All build-verified (2,069 pages green). See Part A.

**Update 2026-07-13 (round 3 — first real CONTENT class):** built the **Fruits category** as the reference implementation for §13–§25 — `/foods` hub, `/foods/fruits` hub (comparison table + ItemList schema), and **12 real fruit pages** (mango, banana, guava, papaya, pomegranate, amla, apple, watermelon, orange, jackfruit, pineapple, sapota) with real USDA/IFCT per-100g composition, glycemic index, evidence-graded (A–D) health notes, clinical reviewer, and citations. New reusable `FoodEntity` data model (`src/lib/foods_database.ts`) + `getFoodEntityGraph` schema generator. Added §44 related-links to topic (disease) pages. Foods added to nav; `.htaccess` /foods conflict fixed. **Verification:** food data + schema logic verified via `tsx` (12/12 pass, JSON-LD valid with MedicalWebPage+Physician+Thing+Claim); topic related-links logic verified (242/242). **Full 2,069-page `astro build` could not complete this session** — the sandbox OOM-hangs generating the 1,701 clinic pages (environmental, unrelated to these changes); templates mirror proven patterns. Rebuild on a normal machine to confirm end-to-end. **No fabricated data or citations** — composition traces to USDA FDC IDs; unverified study citations are marked `citationPending`, not invented.

**Content classes status (§13–§32):** Fruits ✅ (12 pages, reference impl). Remaining classes (vegetables, seeds, spices, grains/millets, pulses, oils, dairy, beverages, functional, fermented, vitamins, minerals, phytonutrients, bioactives, organs, life-stages) = same pipeline, need their data authored batch-by-batch (real sourcing). This is inherently multi-session; ~2,200 pages cannot be responsibly emitted at once.

**Update 2026-07-15 (round 4 — major discovery: a second, much larger content system now exists):** Between sessions, a `src/content/knowledge/` Astro **content collection** appeared (2,145 markdown files, driven by `src/content.config.ts` + `src/lib/seo/taxonomyEngine.ts` + the catch-all route `src/pages/knowledge/[...slug].astro`), sourced from three docx outlines at the project root (`Health Knowledge Hierarchy*.docx` — confirmed as the literal generation spec, 21 pillars, ~1,140 planned pages). This is a completely separate system from the `foods_database.ts` / `/foods/fruits` pages built in round 3. Findings, all build/script-verified this session:

1. **Routing bug (mechanical, fixable without content decisions):** `taxonomyEngine.ts`'s `getTaxonomyDetails()` only branches on 10 of the 21 planned pillars (diseases, foods, herbs, vegetables, fruits, seeds, spices, grains, pulses-legumes, dairy-alternatives, healthy-drinks-beverages). Content under `organ-wise/`, `life-stage/`, `Functional Foods/`, `Fermented Foods/`, `Oils-Healthy-Fats/`, `Nutrient Database/`, `vitamins/`, `phytonutrients/`, `bioactive-compounds/` has no matching branch and falls through to a generic `/knowledge/foods/general-nutrition/general-science/*` bucket — or, for `organ-wise/` specifically, gets accidentally swallowed into the **Diseases** pillar because organ-name subfolders (e.g. "Blood & Blood Cells") trigger disease-keyword matches. Confirmed via a real `astro build` from a clean `/tmp` copy (bindings reinstalled for linux-arm64; 0 `astro check` errors).
2. **Real duplicate-topic content, not just routing:** e.g. `Blood Disease/Blood & Blood Cells/org-blood-1.md` and `organ-wise/Blood & Blood Cells/org-blood-1.md` are ~99% identical (only the category label differs) — the same generator pass appears to have stamped the same topic into two different top-level pillar folders. 258 filename-collision groups (546 files) detected across the tree; scope of true content duplication not fully mapped.
3. **Content quality: ~99.9% of the 2,145 files are template-boilerplate, not researched content.** A script check found 2,142/2,145 files contain the same generated-template language ("is a critical clinical pathway within **X** under the **Y** domain…" / "is a highly valued nutritional agent…") with no real per-food composition numbers, no real biomarkers — only generic placeholder tables ("High concentration of active polyphenols", "Follows standard RDI guidelines"). Only **3 files in the whole tree** contain anything citation-like (`References:`/PMID/doi). This does not meet the evidenced, cited, non-fabricated bar used for the 12 Fruits pages in round 3.
4. **A file changed mid-session:** `Grains/rice.md` was read early this session with real, specific content (indica/japonica varieties, actual kcal/fiber table, arsenic-mitigation guidance, dated citations). Re-read later in the same session, it now contains only the generic template text described in point 3. Root cause not confirmed (possibly a bulk regeneration pass overwriting already-good files) — flagged to the owner rather than guessed at.
5. **`/foods/fruits/*` (round 3, 12 pages, real USDA/IFCT-sourced) now duplicates topically with `/knowledge/fruits/...citrus-fruits/...` etc. (templated, uncited).** Two different quality tiers of the same subject now live at two different URLs — an SEO duplicate-content and information-quality risk until resolved.

No code or content was deleted or overwritten to "fix" this — these are findings only, reported to the owner for a decision (Part D1) before further mass action, per standing instruction not to guess on architecture calls affecting thousands of files.

**Update 2026-07-15 (round 5 — owner decisions + entity auto-linker, §44):** Owner decided: (1) rewrite `src/content/knowledge/` batch-by-batch with real cited content — boilerplate is scaffolding only; (2) `/knowledge/*` is canonical — `/foods/fruits/*` (round 3) will fold into it. Built `src/lib/seo/autoLinker.ts` — a registry-backed entity linker over herbs/spices, conditions, topics, and fruits (286 entities) with `findEntityUrl()` (exact lookup for structured items), `linkifyText()` (first-mention prose linking, skips existing `<a>` tags), and `getLocationsForFood()` (reverse lookup — which cities list a food as a local specialty). Wired into: recipe pages (ingredient list links to herb/spice/fruit pages; `fullHealingContent` prose auto-links first mention of each ingredient), clinic pages both `[id].astro` and `[city]/[disease].astro` (local-produce chips link to food pages; disease name links to its topic page), and fruit pages (new "Where {fruit} is a local specialty" section reverse-links to the ~1,700 city clinic pages that list it). This is two-way: location → food and food → location, per owner's explicit ask, and also serves as a differentiator against the clinic-page thin-content risk (§5.5/§40.5) since each city page now carries genuinely different linked produce data. Verified via `astro check` (0 errors) and a `tsx` logic test (`scripts/_test-autolinker.ts`, all lookups + reverse-lookup correct). Also fixed food-pillar cross-linking per owner request: every food-type pillar hub (Fruits, Vegetables, Grains, etc.) now shows a "Browse Other Food Categories" strip, and `/knowledge` groups pillars into Food & Nutrition / Diseases & Conditions / Body & Life Stage sections.

**Update 2026-07-15 (round 6 — retired /foods/fruits/*, all real content now lives in /knowledge/):** Migrated all 12 real, USDA/IFCT-cited fruit pages from `foods_database.ts` into real markdown at `src/content/knowledge/Fruits/...` (`scripts/_migrate-fruits-content.ts`), replacing the boilerplate stubs that were there. Confirmed canonical URLs by tracing `taxonomyEngine.ts` routing (e.g. mango → `/knowledge/fruits/tropical-exotic-fruits/creamy-dense-tropicals/mango`). Retired `/foods/fruits/[id].astro` and `/foods/fruits/index.astro` into redirect stubs (meta-refresh + `rel=canonical`, `noindex,follow` — not deleted, so any existing inbound link still resolves) and added matching `.htaccess` 301s for all 12 fruits + the hub as the production-grade redirect. Updated every internal reference to the old URLs: `autoLinker.ts`'s fruit registry (`FRUIT_KNOWLEDGE_URL` map — cascades automatically to every page using `findEntityUrl`/`linkifyText`, including recipes and clinic pages), header nav, and `/foods` hub's Fruits card. Also fixed two stale `.htaccess` redirects left over from before the taxonomy fix (`/knowledge/vitamins` was wrongly 301'ing away from the now-real vitamins pillar; `/nutrients` pointed at the old `health-topics` slug). Verified via `astro check` (0 errors) and a direct `tsx` call confirming `findEntityUrl('Mango')` etc. now resolve to the new `/knowledge/...` URLs.

**Update 2026-07-15 (round 7 — Grains, second real CONTENT class):** Added `GRAINS_DATA` to `foods_database.ts` — 9 real, USDA FoodData Central / IFCT 2017-sourced grain/millet entries (rice, wheat, oats, barley, bajra/pearl millet, ragi/finger millet, jowar/sorghum, proso millet, quinoa) with per-100g composition, glycemic index, evidence-graded health notes, and citations (FDC IDs verified via WebSearch: rice 169756, oats 169705, barley 170284, proso millet 169702, quinoa 168874; wheat/bajra/ragi/jowar cited to IFCT 2017 as no single confirmed FDC ID was verified for the whole-grain/Indian forms). Migrated this data directly into the **existing scientific-name-slugged files already sitting in `src/content/knowledge/Grains/`** (`scripts/_migrate-grains-content.ts`) — e.g. `Pennisetum & Eleusine Genus/eleusine-coracana.md` for ragi — replacing their boilerplate with real content, deliberately scoped to only the 9 genuine grain files and leaving the ~100+ misfiled non-grain subfolders in `Grains/` (spices, pulses, beverages — round-4 finding) untouched. Added `GRAIN_KNOWLEDGE_URL` to `autoLinker.ts` (registry now 303 entities) with both scientific/common name ("Pearl Millet") and regional name ("Bajra") as separate linkable aliases to the same URL. Verified via `astro check` (0 errors) and direct `tsx` lookups (Quinoa/Bajra/Ragi/Rice/Wheat all resolve correctly) — confirms, e.g., the "Golden Turmeric Quinoa Bowl" recipe now auto-links both Quinoa and Turmeric ingredients. **Partial real `astro build` also run** (clean `/tmp` copy, native linux-arm64 bindings reinstalled): 2,002 log lines generated with **zero `[ERROR]` entries** before the 44s sandbox timeout cut it off (same known sandbox limitation as prior rounds — 2,000+ pages exceeds one call's budget) — confirmed the new pillar routing rendering correctly (e.g. `bioactive-compounds/omega-3-marine-bioactives/...` instead of the old general-nutrition bucket) and the `/foods/fruits/mango` redirect stub building successfully. Build did not reach the Grains/Fruits pages alphabetically before cutoff; full end-to-end confirmation still needs a normal-machine build.

**Update 2026-07-15 (round 8 — recipes + two-way recipe/food/location interlinking):** Per owner request ("create more recipes pages and locations details like favourite and famous dish/recipes/food/fruits etc. and wise versa pages like mango shows and links in details like where it produce/origin/consumed"): (1) Confirmed `src/lib/recipes.ts` and `src/data/recipes.ts` are both symlinks to the single real file `src/lib/recipes_database.ts` — no duplication to fix. (2) Added **8 new recipes** (mango-ginger-overnight-oats, pomegranate-ragi-porridge, watermelon-mint-cooler, jowar-roti-curry-leaf-tempering, papaya-turmeric-smoothie-bowl, bajra-khichdi-garlic-tempering, guava-mint-fresh-salad, barley-pineapple-detox-bowl — total now 48), each deliberately built from ingredient names that resolve in the `autoLinker.ts` registry (grains from round 7, fruits from round 3/6, herbs/spices already in `clinical_databases.ts`) so every new recipe is auto-interlinked on creation. (3) Added two reverse-link helpers to `autoLinker.ts`: `getRecipesForFood(name)` (food → recipes that use it as an ingredient) and `getRecipesForLocation(loc)` (a city's local fruits/vegetables → recipes built around them) — both strictly data-driven off real `RECIPES` ingredient matches, no invented per-city "famous dish" claims. (4) Wired into pages: the `/knowledge/...` article template (`[...slug].astro`) now shows a "Recipes Featuring {food}" section and a "Where It's Grown & Consumed" location-chip section on every food-pillar article (fruits, grains, vegetables, etc. — extends automatically as more pillars get real content); both `clinic/[id].astro` and `clinic/[city]/[disease].astro` now show a "Local Recipes & Favourite Dishes" chip section next to the existing local-produce chips — completing the two-way loop the owner asked for (mango page → local recipes/growing regions; city page → favourite dishes reverse-linking back to those food pages). **Verified:** `astro check` (0 errors, 132 files); partial real `astro build` (703 log lines before 40s sandbox cutoff, 0 `[ERROR]` entries, only pre-existing duplicate-slug `[WARN]`s from the round-4 content-scatter finding, unrelated to this round); direct `tsx` logic test (`scripts/_quick-check-recipes.ts`) confirming all 8 new recipes present, `getRecipesForFood`/`getRecipesForLocation` resolve correctly (e.g. Mango → `mango-cardamom-lassi` + `mango-ginger-overnight-oats`; a mock Mango/Guava location → both plus `guava-mint-fresh-salad`).

**Update 2026-07-15 (round 9 — Vegetables, third real CONTENT class):** Added `VEGETABLES_DATA` to `foods_database.ts` — 12 real, USDA FoodData Central-sourced vegetable entries (spinach, kale, cabbage, broccoli, cauliflower, carrot, beetroot, onion, tomato, potato, sweet potato, eggplant) with per-100g composition, glycemic notes, evidence-graded health notes, and citations (FDC IDs verified via WebSearch: spinach 168462, kale 168421, cabbage 169975, broccoli 170379, cauliflower 169986, carrot 170393, beetroot 169145, onion 170000, tomato 170457, potato-boiled 170438, sweet-potato-baked 168483, eggplant 169228). Also verified via WebSearch and corrected a well-known nutrition myth in the carrot entry: carrots are commonly miscited as GI ~92 from an early-1980s error; the current International/Sydney reference values are raw ≈16, cooked ≈39 (moderate), with glycemic load staying low regardless because carrots are ~88% water — documented in the entry rather than repeating the myth. Similarly documented that potato is genuinely high-GI (≈78–96, real, not a myth) and that sweet potato's GI swings sharply by cooking method (baked ≈94 vs. boiled ≈44) — both clinically actionable preparation notes. Audited `src/content/knowledge/Vegetables/` (38 files, 14 subfolders) and found it clean, unlike the round-4 Grains scatter — only 4 legacy common-name-slugged duplicate files (kale.md, cabbage.md, broccoli.md, eggplant.md, plus an orphan Leafy-Greens/spinach.md) sit alongside the scientific-name files the 12 real entries were migrated into; left untouched and flagged for owner dedup, consistent with the round-7 approach. Migrated via `scripts/_migrate-vegetables-content.ts` into the existing scientific-name-slugged files (e.g. `Solanaceae Crops/solanum-lycopersicum.md` for tomato). Added `VEGETABLE_KNOWLEDGE_URL` to `autoLinker.ts` (computed by tracing `taxonomyEngine.ts`'s vegetable-pillar branch — root/tuber, cruciferous, leafy-green, fruiting, bulb-stem sub-routing) plus romanized regional-name aliases (e.g. "Palak" for spinach) pulled from each entry's `regionalNames.hi` field, so recipe ingredients and location produce chips using either the English or regional name auto-link correctly. Because `vegetables` was already in `[...slug].astro`'s `FOOD_FAMILY_PILLARS` list (round 5), every new vegetable article automatically got the "Recipes Featuring X" and "Where It's Grown & Consumed" sections built in round 8 — no template change needed. **Verified:** `astro check` (0 errors, 132 files); a direct `tsx` logic test (`scripts/_quick-check-vegetables.ts`) confirming all 12 computed URLs match `findEntityUrl()` output exactly, no name collisions with existing herb/fruit/grain entities, and `getRecipesForFood`/`getLocationsForFood` resolve against real data; partial real `astro build` (2,145 log lines before the 40s sandbox cutoff, 0 `[ERROR]` entries) with content-collection sync logs confirming all 12 new files loaded without a parse/duplicate-id warning (the only Vegetables-folder duplicate-id warning was for the pre-existing, untouched `cucurbita-pepo.md`).

**Update 2026-07-15 (round 10 — Spices, fourth real CONTENT class, plus a real bug fix in the auto-linker):** Before writing content, audited `src/content/knowledge/Spices/` (39 files) against `HERBS_SPICES_DATA` in `clinical_databases.ts` and found a **major overlap**, structurally different from the Grains/Vegetables scatter: `HERBS_SPICES_DATA` already has real, cited, clinically substantial monographs (dosage, contraindications, mechanism, PMIDs) at `/herb/{id}` for turmeric, cinnamon, ginger, fenugreek, garlic, cumin, (green) cardamom, clove, black pepper, asafoetida, saffron, mustard seeds, and star anise — the exact same items the boilerplate `Spices/` folder covers. Unlike the round-4 Fruits case, `/herb/{id}` here is the good, real content (not the thing needing replacement), so building a second `SPICES_DATA`/`FoodEntity` entry for any of these would recreate the round-4 duplicate-canonical problem in reverse. **Decision (autonomous, low-risk): left every overlapping file untouched** and scoped this round strictly to 5 spices with no existing `/herb/` page: coriander seed (FDC 170922), fennel seed (171323), nutmeg (170932), allspice (171315), and white pepper (170933 — botanically the same fruit as black pepper but a nutritionally/culinarily distinct USDA entry due to processing). **Declined to fabricate entries for 5 other candidates** (ajwain/carom, black cardamom, long pepper/pippali, galangal, cayenne) because the only numbers found for them traced to unverified blogs, not a confirmed USDA FDC ID or IFCT figure — below this project's citation bar; flagged in Part D1 for future sourcing rather than guessed. Migrated the 5 real entries via `scripts/_migrate-spices-content.ts` into their scientific-name-slugged files (e.g. `Piperaceae Berries/piper-nigrum-white.md`) and added `SPICE_KNOWLEDGE_URL` to `autoLinker.ts`. **While verifying no name collisions, found and fixed a real pre-existing bug**: 9 of the 13 `HERBS_SPICES_DATA` entries carry a regional-name parenthetical in their `name` field (e.g. `"Cumin (Jeera)"`, `"Cardamom (Elaichi)"`, `"Ceylon Cinnamon"`) that the registry builder never stripped — meaning a plain "Cumin" or "Cardamom" mention in any recipe or prose (including several of this session's own new recipes) was silently failing to link at all since round 5. Fixed `buildRegistry()`'s herb/spice loop to strip the parenthetical (mirroring the pattern already used for `MEDICAL_CONDITIONS_DATA`) and also register the bare regional alias (e.g. "Jeera" now separately links to `/herb/cumin`) — this one fix retroactively repairs interlinking across every recipe and clinic page already shipped, not just this round's additions. **Verified:** `astro check` (0 errors, 132 files); a direct `tsx` logic test (`scripts/_quick-check-spices.ts`) confirming all 5 new URLs resolve correctly AND confirming all 13 previously-broken herb/spice names now resolve to their correct `/herb/{id}` pages; partial real `astro build` (2,119 log lines before the 40s cutoff, 0 `[ERROR]` entries, no warnings on any of the 5 new files).

**Update 2026-07-15 (round 11 — triple-name title cleanup + a site-wide critical SEO fix found during verification):** User asked whether the "Cumin (Jeera)" regional-name parenthetical also shows up in rendered pages (not just the internal auto-link registry touched in round 10) and, seeing that it did, asked for a site-wide audit and clean fix wherever regional + common + scientific names were being crammed into one title/heading. Grepped every `.astro`/`.tsx` file for the pattern and found it in exactly **6 call sites across 3 files** (not actually "4000+" distinct occurrences — the underlying data (13 `HERBS_SPICES_DATA` entries) is small, but confirmed via full-codebase grep that these were the only offenders; `ClinicalDetails.tsx`'s H1 and the `/knowledge/herbs`/`/knowledge/spices` listing cards, `faqHelper.ts`, and `glossary.astro` were already showing scientific name *separately* from the common name, i.e. already correct): `src/pages/herb/[id].astro` (schema `name`, `<title>`, and an H2 all concatenated `{name} ({scientificName})` on top of a name that already had a regional parenthetical, e.g. rendering "Cumin (Jeera) (Cuminum cyminum)"), `src/pages/sitemap.astro` (2 identical labels for the herbs/spices sitemap sections), and `src/components/ClinicalDetails.tsx` (one summary sentence). Fixed each using a "clever, not crammed" pattern per the owner's instruction that many visitors don't recognize scientific names: SEO `<title>` now drops the scientific name entirely (`{name} | Clinical Monograph | NutritionColours` — nobody searches Latin binomials); the JSON-LD schema now uses the proper `alternateName` field instead of string-concatenating (`"name": herb.name, "alternateName": herb.scientificName`); the on-page H2 keeps the scientific name but demotes it to a separate small italic line beneath the heading instead of nesting a second parenthetical; the sitemap label and the `ClinicalSummaryBox` sentence both use an em-dash/prose separator ("Cumin (Jeera) — Cuminum cyminum" / "Cumin (Jeera), scientifically Cuminum cyminum,...") instead of double parens. **While verifying the fix by inspecting real built HTML output, found a much larger, previously-undiscovered site-wide bug**: `src/components/Seo.astro` (rendered inside `BaseLayout.astro`'s `<head>` on every single page) sets `og:title` and `twitter:title` meta tags but **never renders an actual `<title>` HTML element** — confirmed by extracting the built `<head>` for `/herb/cumin` and finding zero document `<title>` tags (the only `<title>` in the whole page was an unrelated SVG icon's accessibility title). This means every page on the site (~4,000+) has been shipping without a browser-tab title and, more importantly, without the on-page title signal Google primarily uses for search result headlines — a significant, site-wide SEO gap, unrelated to what was asked but caught in the course of verifying this fix. Added the missing `<title>{title}</title>` to `Seo.astro`; confirmed no other layout already renders one (would have caused a duplicate). **Verified:** `astro check` (0 errors, 132 files); partial real `astro build` (2,052 log lines, 0 `[ERROR]`); inspected the actual built `/herb/cumin/index.html` — confirms the page now renders a correct, page-specific `<title>` (picking up each herb's existing custom `pageTitle` where set, e.g. "Cumin (Jeera) Clinical Guide – Enzyme Activation & Weight Loss", with no double-parenthetical anywhere), the H2 and scientific-name caption render correctly separated, the JSON-LD schema correctly splits `name`/`alternateName`, and `og:title` is unaffected.

**Update 2026-07-15 (round 12 — full 21-pillar audit for the same crowding pattern, per owner request):** Owner asked to confirm every pillar was checked, not just herbs — Diseases, Foods, Herbs, Vegetables, Fruits, Seeds, Spices, Grains, Pulses & Legumes, Oils & Healthy Fats, Dairy & Alternatives, Drinks & Beverages, Functional Foods, Fermented Foods, Nutrient Database, Vitamins, Minerals, Phytonutrients, Bioactive Compounds, Organ-wise Knowledge, and Life-stage. Ran one exhaustive regex (`^title:.*\([^)]+\).*\([^)]+\)`) across every markdown file in `src/content/knowledge/` (all pillar folders, ~2,145 files) rather than checking folder-by-folder, so the sweep is complete regardless of naming. **Found the exact same bug I'd just fixed in herb pages was also present in my own round-3/round-7 work**: 8 of 9 Grains files and 2 of 12 Fruits files had scientific name double-parenthesized on top of a `name` that already carried a regional/form alias (e.g. `"Sorghum (Jowar) (Sorghum bicolor)"`, `"Sapota (Chikoo) (Manilkara zapota)"`, `"Rice (White, Long-Grain) (Oryza sativa)"`) — same root cause as the herb bug, just in the content-collection frontmatter `title` field instead of a page template. Vegetables and Spices (rounds 9–10) were already clean because those used a separate `regionalNames.hi` field instead of embedding the alias in `name` — confirmed no fix needed there, but **standardized the title-construction logic across all 4 food migration scripts anyway** for consistency and future-proofing. Fix applied in `scripts/_migrate-{fruits,grains,vegetables,spices}-content.ts`: title is now always `"{name} — Nutrition, Composition & Health"` (scientific name dropped from the human-facing title entirely), with scientific name preserved as (a) a separate italic line under the Overview heading (`*Scientific name: Sorghum bicolor*`) and (b) a `seoKeywords` entry — same "keep it discoverable, don't cram it" pattern as the herb fix. Re-ran all 4 scripts, regenerating all 38 real content files (12 Fruits + 9 Grains + 12 Vegetables + 5 Spices) with the corrected format. **Checked but found clean, no fix needed:** Diseases (`condition/[id].astro` and `topic` pages only ever use the medical name's own single alias, e.g. "MASLD (Fatty Liver)", never a second appended technical/ICD name — verified via direct grep of the page templates); the 14 still-boilerplate pillars (Seeds, Pulses & Legumes, Oils & Healthy Fats, Dairy & Alternatives, Drinks & Beverages, Functional Foods, Fermented Foods, Nutrient Database, Vitamins, Minerals, Phytonutrients, Bioactive Compounds, Organ-wise, Life-stage) use plain descriptive English titles with no scientific-name convention at all, so the bug pattern doesn't apply to them (separately, these are still round-4's flagged 99.9%-boilerplate content awaiting real-data rewrites — an unrelated, already-tracked issue, not a title-formatting one). The only remaining 2-parenthetical titles anywhere in the full tree (re-verified via the same regex after the fix, down from 15 matches to 5) are legitimate abbreviation-expansion patterns with no redundancy — "HIV: Transmission, Testing (4th Gen) & Antiretroviral Therapy (ART)", "Hepatitis C (HCV)...", "Primary Biliary Cholangitis (PBC) & Primary Sclerosing Cholangitis (PSC)" — left untouched as correct. Re the owner's separate note that location pages exist to combine local results for UX/SEO/AEO/GEO/AIO: acknowledged — no action needed here, this confirms the rationale behind the recipe/food/location interlinking work already shipped in rounds 8–10. **Verified:** `astro check` (0 errors, 132 files); partial real `astro build` (reached deeper into the tree this run — 2,464+ log lines, 0 `[ERROR]`, confirmed the specific regenerated files like `sapota-chikoo` and `sorghum-bicolor` build successfully); inspected actual built HTML `<title>` tags for both, confirming clean single-parenthetical titles with no redundancy (e.g. `"Sorghum (Jowar) — Nutrition, Composition & Health | Coarse & Small Grains Protocol"`).

**Update 2026-07-15 (round 13 — Seeds, fifth real CONTENT class):** Audited `src/content/knowledge/Seeds/` (31 files, 13 subfolders) and found the round-4-style cross-pillar duplication pattern again: cumin, coriander, and fennel each have a *second* boilerplate file here in addition to their Spices/herb-page coverage, and quinoa has a *third* copy here on top of Grains — deliberately left all of these untouched this round (flagged, not fixed) rather than picking a winner without an owner decision. Also skipped lentils, chickpeas, common beans, and green peas even though files exist here, since those are legumes/pulses, not oilseeds, and are better scoped to a future dedicated Pulses & Legumes round rather than folded in here. Scoped this round to **11 genuinely non-overlapping seeds**: pumpkin seeds (FDC 170556), sunflower seeds (170562), flaxseed (169414), chia seeds (170554), sesame seeds (170150), psyllium husk/isabgol (1666127 — a less-certain FDC ID, but the ~350 kcal/high-fiber figures are independently well-corroborated so kept, unlike the ajwain/cayenne-class rejections in round 10), hemp seeds (170148 — explicitly noted as non-psychoactive, food-grade hulled seed, distinct from the plant's regulated parts), watermelon seeds (169407), buckwheat/kuttu (170685), amaranth/rajgira (170682), and poppy seed/khus khus (171330 — noted as the legal culinary seed with only trace, pharmacologically negligible alkaloid residue, not the opium latex). Added `SEEDS_DATA` to `foods_database.ts` and a `scripts/_migrate-seeds-content.ts` migration script built with the round-11/12 title fix already applied from the start (title never includes scientific name; it's a separate italic line + a keyword instead) — no retrofit needed this time. Added `SEED_KNOWLEDGE_URL` to `autoLinker.ts` with regional aliases (Alsi, Til, Isabgol, Kuttu, Rajgira, Khus Khus) resolving to the same URLs as their English names. **Verified:** `astro check` (0 errors, 132 files); a direct `tsx` logic test (`scripts/_quick-check-seeds.ts`) confirming all 11 URLs resolve correctly, all 6 regional aliases work, and no collisions against any of the 55 entities added in prior rounds (fruits, grains, vegetables, spices, herbs); partial real `astro build` (1,531 log lines, 0 `[ERROR]`, no duplicate-id warnings for any of the 11 new files in the content-collection sync log).

**Update 2026-07-15 (round 14 — Pulses & Legumes, sixth real CONTENT class):** Added `PULSES_DATA` to `foods_database.ts` — 10 real, USDA FoodData Central-sourced entries (chickpeas FDC 173757, lentils 172421, kidney beans 173740, mung beans 174257, pigeon pea/toor dal 172437, green peas 170420, peanuts 173806, soybeans 174299, fava beans 173753; urad dal/black gram cited to IFCT 2017 as no clean FDC ID was verified, same disclosed-substitution pattern as round-7 wheat/bajra/ragi) with per-100g composition, glycemic index, evidence-graded health notes, and two clinical safety flags surfaced in `healthNotes`/`flags` rather than buried: kidney beans require thorough cooking (raw/undercooked phytohaemagglutinin toxin risk) and fava beans carry a G6PD-deficiency/favism caution. Cross-referenced round 13's finding that chickpeas, lentils, common beans, and green peas already have boilerplate duplicate files sitting in `Seeds/` — this pillar is their intended canonical home; the `Seeds/` copies are left untouched and flagged, not overwritten. Migrated via `scripts/_migrate-pulses-content.ts` into the existing scientific-name-slugged files (e.g. `Tropical & Subtropical Pulses/cajanus-cajan.md` for pigeon pea), built with the round-11/12 title standard from the start (no scientific name in `title`; surfaced as a separate italic Overview line + a `seoKeywords` entry instead). 15 additional duplicate/variant files (color/form variants of the same 10 items, e.g. black/brown/green whole lentils, black-eyed peas, adzuki beans) found and left untouched, flagged in `KNOWN_DUPLICATES` and Part D1 rather than guessed at. Added `PULSE_KNOWLEDGE_URL` to `autoLinker.ts` (registry now ~76 entities) with regional-name aliases (e.g. "Chana" for chickpeas, "Toor Dal" for pigeon pea, "Black Gram" for urad dal) resolving to the same URLs. **Verified:** `astro check` (0 TypeScript errors across 245 output lines; hit the 40s sandbox timeout on the full 2,000+ page project before finishing, same accepted partial-check precedent as every prior round); a direct `tsx` logic test (`scripts/_quick-check-pulses.ts` + a follow-up variant isolating base-name/alias lookups) confirming all 10 entries resolve correctly via `findEntityUrl()` with no collisions against any previously-registered entity — the two items with regional parentheticals in their `name` field (Urad Dal (Black Gram), Pigeon Pea (Toor Dal)) correctly register as two separate linkable aliases each, by the same design as the round-10 herb/spice fix, not a bug; partial real `astro build` (0 `[ERROR]` entries; the only duplicate-id `[WARN]`s in the log are pre-existing ones unrelated to this round — adaptogens, functional-foods, herbs, Coriandrum Sativum — confirming none of the 10 new pulse files triggered a collision).

This session's content build now covers **6 real, cited food pillars** (Fruits, Grains, Vegetables, Spices, Seeds, Pulses & Legumes — 71 individually sourced food entities total) with a consistent title-formatting standard, a unified auto-linking registry, and two-way recipe/location interlinking. Remaining unstarted pillars per the round-4 baseline: Oils & Healthy Fats, Dairy & Alternatives, Drinks & Beverages, Functional Foods, Fermented Foods, Nutrient Database, Vitamins, Minerals, Phytonutrients, Bioactive Compounds, Organ-wise Knowledge, Life-stage.

**Update 2026-07-15 (round 15 — Oils & Healthy Fats audit, session's WebSearch quota exhausted before content could be written):** Audited `src/content/knowledge/Oils-Healthy-Fats/` (19 files, 15 subfolders) ahead of writing real content and found a structural problem worse than any prior pillar's duplication: nearly the entire subfolder tree (13 of 15 subfolders — smoke-point tiers, extraction methods, storage/rancidity, industrial/biofuel uses, cosmetic uses, consumer decision frameworks, etc.) is **cloned verbatim into `organ-wise/`** (confirmed via `diff` — files are byte-identical except for the `category:` frontmatter field, e.g. `oils-healthy-fats` vs. `organ-wise`), the same "same generator pass, two pillar labels" pattern first found in round 4, now confirmed specifically in this pillar too. Separately, 6 of those subfolders (Cooking Techniques & Oil Selection, Cosmetic & Skincare Oils, Environmental & Ethical Considerations, Industrial Seed Oils & Byproducts, Industrial/Biofuel Applications, Traditional & Cultural Oil Uses) have **no copy inside `Oils-Healthy-Fats/` at all** — they exist only under `organ-wise/` and a third folder, `health conditions/` — meaning this pillar's own content is partly missing from its own folder and stranded in two unrelated pillars, a `taxonomyEngine.ts` routing/misfile issue distinct from the duplication. Also found the pillar's content architecture is **cluster-based, not atomic-per-food** like every pillar built so far: most subfolder files (`oil-cook-1.md`, `seed-oil-1.md` through `seed-oil-5.md`, `oil-spec-1.md`, etc.) are titled and written as multi-oil comparison narratives (e.g. `seed-oil-2.md` = "Sesame: White, Black, Brown, Tahini & Gomasio", `oil-cook-2.md` = "Medium Smoke Point Oils: Olive Oil, Sesame Oil, Coconut Oil, Palm Oil") rather than one file per single oil — only 2 of the 19 files (`Cold-Pressed-Oils/extra-virgin-olive-oil.md`, `Tropical-Oils/coconut-oil.md`) are named after and scoped to one specific oil, matching the `FoodEntity`/single-item migration pattern used for the 6 pillars built so far. Rewriting any of the cluster files with real per-oil data for just one of the several oils named in their title would misrepresent the file's stated scope — flagged as an architecture question rather than guessed at. **Content work for this round could not proceed**: `WebSearch` returned "You've hit your weekly limit · resets Jul 18 at 7:30pm" on the first two lookups (olive oil and coconut oil FDC IDs) — per this project's standing anti-fabrication rule, composition numbers are never taken from memorized/unverified training-data recall, so no `OILS_DATA` entries were written this round. No files were changed. **Resume once WebSearch quota resets (2026-07-18) or the owner supplies sourced figures directly** — starting with the 2 clean atomic targets (extra virgin olive oil, coconut oil), then getting an owner ruling on the cluster-vs-atomic architecture question before attempting mustard oil / ghee / groundnut oil / sesame oil / sunflower oil, all of which currently only exist inside multi-oil cluster pages.

---

## Part A — ✅ DONE this engagement (autonomous, build-verified)

Code, verified against a green 2,069-page build + `astro check` 0/0 + JSON-LD gate:

- **§46.1–§46.4** — medical JSON-LD on every YMYL template; **§46.4** now enforced by `scripts/validate-jsonld.mjs` (4,648 blocks valid).
- **§12 (schema portion)** — 229 disease pages upgraded to `MedicalWebPage`+`Physician`+`MedicalCondition`+`Claim`+`BreadcrumbList`+`Speakable` via `getTopicEntityGraph`.
- **genomics/interactions schema** (§46.3, §4.4b) — `Gene`/`Drug`+`DrugClass`+reviewer+`Claim`.
- **§42.1** — `llms.txt` re-pointed to on-site money pages.
- **§7 (404)** — custom 404 enriched to full hub recovery (Diseases, Herbs, Recipes, Tools, Plans + topic cards).
- **§65.5** — 4 pre-existing `tsc` errors fixed; duplicate-topic-ID route collisions removed; symlink/dup myth corrected.
- **§12.1 (label)** — "Health Topics → Diseases" relabel site-wide (URLs preserved).
- **§72.1 / §72.4** — CI quality gates wired: `scripts/validate-jsonld.mjs` + `.github/workflows/ci.yml` (check → build → schema gate); JSON-LD gate run-verified.
- **§0.x** — MRS maintained (v1.1, Owner/Author = Dr. Shilpa Thakur (PhD)).
- **§2.2–§2.6** — audited before proposing; corrected own wrong findings (symlink, data counts, stale-docs).
- **§44.5** — orphan-inlink checker (`scripts/check-orphan-links.mjs`, `npm run check-orphans`); run-verified — found **377 pages with <2 contextual inlinks** (217 topic, 36 recipe, 31 herb) → feeds the §44.1 link-engine work.
- **§10.5** — glossary expanded to **288 real `DefinedTerm` entries** (conditions + herbs + 229 diseases + genomics + interactions, deduped; no fabrication) — near the ≥300 target; build-verified.
- **§42.6 + §7** — `public/.htaccess`: custom-404 wired on Apache + hallucinated-URL 301 capture with 30+ safe starter mappings (all targets verified real); deploys in dist. Weekly log-grep process documented in-file.
- **§43.5** — View-Source audit: YMYL knowledge pages are crawler-visible (client:load SSR + schema); **clinic pages + clinics.astro use `client:only`** → body crawler-invisible (fix tied to §5.5 clinic decision, D1).
- **§40.1/§7.6** — audited: no faceted/filter/search/tag routes exist (nothing to noindex there); sitemaps segmented + honest; thin-content noindex = the clinic §5.5 decision (D1).

## Part B — 📄 DELIVERED as artifacts (autonomous; every sub-point covered in a doc)

Each section's sub-points are specified/answered in a committed deliverable:

| Sub-points | Where |
|---|---|
| §0.1–§0.6, §1.1–§1.5, §2.1–§2.8, §3.1–§3.3, §6.1–§6.4 | `MASTER_REQUIREMENT_SPECIFICATION.md` |
| §4.1–§4.8 | `docs/audit/VERIFIED_AUDIT_2026-07-12.md` + `existing-state.json` |
| §5.1–§5.5 | `docs/analysis/gap-analysis.md` (incl. missing-content inventory §5.4) |
| §7.1–§7.7, §9.1–§9.5, §11.1–§11.5 | `docs/spec/master-taxonomy.md` |
| §8.1–§8.6, §45.1–§45.4 | `docs/spec/knowledge-graph-blueprint.md` |
| §10.1–§10.6, §13.1–§13.5, §35–§36 (specs) | `docs/spec/page-template-specs.md` |
| §37.1–§37.5, §38.1–§38.5 | `docs/spec/evidence-grading-and-citations.md` |
| §39.1–§39.7, §41, §43.1–§43.5, §44 (method) | template spec + KG blueprint |
| §54.1–§54.5, §55.1–§55.5, §60.1–§60.5, §56 (method) | `docs/spec/content-production-and-writing.md` |
| §59.1–§59.4, §61.1–§61.3, §68.1–§68.4 | `docs/analysis/competitive-and-growth.md` |
| §62.1–§62.3, §72.1–§72.9, §73.1–§73.15 | `docs/qa/blindspots-risks-qa-audit.md` |
| §63–§66, §69–§70 (specs) | MRS + gap-analysis §4 |
| §71.1–§71.15 (deliverables format) | this register + `docs/` index |
| §74 (phased roadmap) | MRS §74 |
| all §1–§74 dispositions | `docs/execution/EXECUTION_LEDGER.md` |

## Part C — 🛠️ SCHEDULED autonomous code (no input needed; next dev cycles)

These need no decision from you — dev can execute + build-verify:

- **§39.2 / §4.4h** — expand single-H2 templates into a multi-H2 answer-first tree (in the React detail components; do with care + layout check).
- **§44.1** — extend `internalLinkEngine.ts` to add contextual inlinks to the **377 orphan pages** the checker flagged (217 topic, 36 recipe, 31 herb).
- **§50.2/§50.4** — give each tool its own indexable URL + embeddable snippet + shown formula.
- **§56.1–§56.4** — content-decay CI + real telemetry to replace stub JSONs.
- **§65.2** — migrate monolithic data → per-entity content collections (the key scale dependency).
- **§40.1 (partial)** — noindex thin filter/search URLs; **§7.6** faceted views noindex.
- **§46.2** — formalize the `@id` `@graph` generator from the registry + drift gate in CI (§8.3).
- **§10.5** — glossary scaffolding (DefinedTerm template) — content is 🧑, structure is 🛠️.
- **§51.2** — WCAG 2.2 AA pass (alt-text-as-sentences, landmarks, targets).
- **§49.2 / §43.5** — ensure no primary content hidden behind hydration (View-Source audit).
- wire-or-delete the 5 unwired components; de-dup `SemanticCompressionLayer.{astro,tsx}`.

## Part D — 🧑 NEEDS YOUR WORDS (do last, per your instruction)

Nothing here can be completed by code alone — grouped so you can clear them in a batch.

### D1. Owner decisions (rulings)
- **§5.5 / §40.5** — clinic-page thin-content: consolidate to hubs, keep only differentiated pages, or noindex the rest? *(Top priority — blocks mass content scaling.)*
- **§12.1 / §34.1** — canonical model for topic↔condition and goals: fully merge disease corpora + 301 map?
- **§49.1/§49.3** — AI features (HealthAssistant): wire as RAG-grounded/cited/disclaimed, or delete?
- **§52 / §53** — personalization + community: greenlight the experimental scope + moderation model?
- **§66.1** — which monetization paths to pursue (consultations, premium plans, data/API, affiliates)?
- **§64.1/§64.2** — approve building the open dataset / MCP server / original research arm?
- **§67 / §7.1** — approve URL migrations (e.g. `/knowledge/health-topics` → `/diseases/`) with 301s?
- **NEW — content-collection strategy (round 4 finding):** `src/content/knowledge/` (2,145 files) is 99.9% template-boilerplate with almost no real composition data or citations. Keep it as a scaffold and have it rewritten batch-by-batch with real sourced content (same bar as the 12 Fruits pages), or treat it as disposable scaffolding and regenerate differently? Blocks whether I fix `taxonomyEngine.ts` routing now or wait.
- **NEW — canonical fruits/foods system:** `/foods/fruits/*` (12 pages, round 3, real USDA/IFCT data + citations) vs `/knowledge/fruits/...` (templated, uncited) now cover the same topics at different URLs. Which is canonical — extend the round-3 pattern and retire `/knowledge/...` food content, or the reverse?
- **NEW — duplicate content across pillars:** 258 filename-collision groups (546 files) where the same topic exists under two different top-level folders (e.g. `organ-wise/` vs disease-named folders) with only the category label changed. Needs a decision on which copy is canonical before dedup.
- **NEW — herb/spice vs. Spices-pillar overlap (round 10 finding):** 13 items (turmeric, cinnamon, ginger, fenugreek, garlic, cumin, cardamom, clove, black pepper, asafoetida, saffron, mustard seeds, star anise) exist as real, cited clinical monographs at `/herb/{id}` (`HERBS_SPICES_DATA`) AND as boilerplate stubs under `src/content/knowledge/Spices/`. I left the boilerplate files untouched rather than guess — should they be retired/redirected into `/herb/{id}` (mirroring the round-6 Fruits retirement), left as a separate nutrition-composition-only companion page, or something else? This round only added the 5 genuinely non-overlapping spices (coriander seed, fennel seed, nutmeg, allspice, white pepper).
- **NEW — 5 spices without a confirmed citation (round 10 finding):** ajwain/carom seeds, black cardamom, long pepper (pippali), galangal, and cayenne pepper have no confirmed USDA FDC ID or IFCT figure I could verify (only unverified secondary/blog sources) — deliberately not written this round to avoid fabricating numbers. Needs either a confirmed IFCT 2017 page reference or explicit owner sign-off to use secondary sourcing with a citation-pending flag before these are added.
- **NEW — Seeds/Spices/Grains cross-pillar duplication (round 13 finding):** `src/content/knowledge/Seeds/` has its own separate boilerplate files for cumin, coriander, and fennel (on top of their Spices-pillar/herb-page coverage) and for quinoa (on top of Grains) — a third instance of the round-4/round-10 duplicate-content pattern. Left untouched pending an owner call on which pillar should be canonical for items that botanically fit more than one category. Also: lentils, chickpeas, common beans, and green peas have boilerplate files sitting in `Seeds/` even though they're legumes — claimed by the round-14 Pulses & Legumes round as their canonical home; the `Seeds/` copies remain untouched duplicates.
- **NEW — Pulses & Legumes internal duplicate varieties (round 14 finding):** within `Pulses & Legumes/` itself, 15 color/form-variant files were left untouched because the round's 10 `PULSES_DATA` entries deliberately targeted one canonical variety each (e.g. red kidney bean, not black/navy/pinto; red split lentils, not the black/brown/green whole or yellow-split forms; desi chickpea, not kabuli or chana dal) — needs an owner call on whether each variant deserves its own real page or should redirect/fold into the canonical one. Two items have real boilerplate files but no `PULSES_DATA` entry yet at all: black-eyed peas (`Elongated Vigna Pulses/vigna-unguiculata.md`) and adzuki beans (`High-Protein Vigna Grams/vigna-angularis.md`) — flagged as future-round candidates, not fabricated this round.
- **NEW — Oils & Healthy Fats: triple-pillar duplication + cluster-vs-atomic architecture (round 15 finding):** 13 of 15 `Oils-Healthy-Fats/` subfolders are cloned verbatim into `organ-wise/` (byte-identical except the `category` field), and 6 of those subfolders exist ONLY under `organ-wise/` and a third folder, `health conditions/` — not in `Oils-Healthy-Fats/` at all. Needs a ruling on which pillar is canonical (same class of decision as the existing "duplicate content across pillars" item above, now confirmed in a third pillar). Separately, and independent of the duplication: only 2 of the 19 `Oils-Healthy-Fats` files are scoped to one single oil (extra virgin olive oil, coconut oil) — the rest are multi-oil comparison narratives (e.g. one file titled "Medium Smoke Point Oils: Olive Oil, Sesame Oil, Coconut Oil, Palm Oil"). Needs a decision: keep the cluster-narrative format and rewrite each with real comparative data across all named oils, or split into atomic single-oil pages matching the pattern used for Fruits/Grains/Vegetables/Spices/Seeds/Pulses so far? This decides how mustard oil, ghee, groundnut oil, sesame oil, and sunflower oil (all currently only named inside cluster pages, none with their own file) get added.

### D2. Clinical content authorship (needs sourcing + your review — must not be fabricated)
- **§12.2–§12.5** conditions to ≥120 · **§14.1–§14.4** herbs to ≥150 · **§15–§25** new food classes (vegetables, fruits, seeds, spices, grains/millets, pulses, oils, dairy, beverages, functional, fermented) · **§26–§30** nutrient/vitamin/mineral/phytonutrient/bioactive data (RDA/UL, composition) · **§31–§34** organs/life-stages/lifestyles/goals content · **§35.2–§35.3** therapeutic meal plans · **§36.1** recipe enrichment · **§10.5** glossary term definitions · **§37.2** real citations for every new claim · **§38.1** evidence grades per claim.
  *(I built the taxonomy, data model, page templates, and evidence scheme these slot into — you/your clinical team supply the sourced content; I can then wire each batch to pages + schema and build-verify.)*

### D3. Off-site / human / ongoing (no code deliverable)
- **§42.2** reverse-source engineering (query the 4 engines monthly) · **§42.3** consensus-seeding boilerplate everywhere · **§57.1–§57.5** authority/citation flywheel, Wikidata items, review-platform profiles · **§58.1–§58.5** atomization + advocacy cadence · **§47.2/§47.5** author bio `sameAs` + cross-platform entity consistency · **§68.1** competitor AI-citation measurement (needs live tracking) · **§69.2–§69.3** legal/compliance sign-off (FSSAI/ASCI/DPDP/WCAG).

### D4. Your machine (I can't from the sandbox)
- **git baseline commit** of `src/lib` (mount blocks the `unlink` git needs for its index lock). Commands are in the prior message.

---

### How to close Part D fastest
1. Give the **§5.5 clinic ruling** + the **topic↔condition merge** decision (unblocks structure).
2. Hand me **one clinical content batch** (e.g. the millets cluster with sources) — I'll wire it to pages + schema + internal links + llms.txt and build-verify, then repeat per batch.
3. Run the **git commit** on your Mac.

Everything in Parts A–C is either done or dev-schedulable without you.
