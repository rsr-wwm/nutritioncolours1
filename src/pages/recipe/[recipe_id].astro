---
export function getStaticPaths() {
  // Legacy recipes remain source-only until a reviewed recipe registry exists.
  // An empty path set is the publication control; `noindex` is not a review gate.
  return [];
}
---
